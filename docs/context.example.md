# Context

<!-- Example only. Write your own and save as docs/context.md (gitignored). -->

Projects live in ~/code/<group>. The group tells you which context I'm in and which Obsidian vault to use.

- clients: code in ~/code/clients/<name>; notes in the work vault ~/Documents/work-vault
- oss: code in ~/code/oss/<repo>; notes in the personal vault ~/Documents/notes
- sandbox: code in ~/code/sandbox; no dedicated vault
- hobby: the DnD campaign vault ~/Documents/dnd-vault
